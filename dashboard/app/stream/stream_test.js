'use strict';

describe('myApp.stream module', function() {

  beforeEach(module('myApp.stream'));

  describe('stream controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var streamCtrl = $controller('streamCtrl');
      expect(streamCtrl).toBeDefined();
    }));

  });
});