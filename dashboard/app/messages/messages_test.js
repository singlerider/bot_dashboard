'use strict';

describe('myApp.messages module', function() {

  beforeEach(module('myApp.messages'));

  describe('messages controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var messagesCtrl = $controller('messagesCtrl');
      expect(messagesCtrl).toBeDefined();
    }));

  });
});